name: Expo EAS Android Build

on:
  push:
    branches:
      - main
      - dev
      - backlog/*

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout code
      - name: Checkout repo
        uses: actions/checkout@v3

      # 2. Setup Node with cache
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"

      # 3. Cache extra dirs (npm, gradle)
      - name: Cache npm & Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.npm/_npx
            ~/.gradle/caches
          key: ${{ runner.os }}-deps-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-deps-

      # 4. Install dependencies
      - name: Install dependencies
        run: npm ci

      # 5. Setup Expo & EAS CLI via official GitHub Action
      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      # 6. Run EAS build (Android only)
      - name: Build Android APK
        id: eas-build
        run: |
          BUILD_URL=$(eas build --platform android --profile preview --non-interactive --json | jq -r '.[0].artifacts.buildUrl')
          echo "build_url=$BUILD_URL" >> $GITHUB_OUTPUT

           # 7. Get app name from app.config.js
      - name: Read App Name
        id: app-name
        run: |
          APP_NAME=$(node -p "require('./app.config.js').expo.name")
          echo "app_name=$APP_NAME" >> $GITHUB_OUTPUT

      # 8. Upload as GitHub artifact (dynamic name)
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.app-name.outputs.app_name }}-apk
          path: app-preview.apk

      # 9. Send Discord notification
      - name: Discord Notification
        if: always()
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: |
            *${{ steps.app-name.outputs.app_name }} Android Build - Beta*
            Status: **${{ job.status }}**
            ━━━━━━━━━━━━━━━━━━
            ⬇️ [Download Artifact](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            ━━━━━━━━━━━━━━━━━━
            Message: ${{ github.event.head_commit.message }}
            ━━━━━━━━━━━━━━━━━━━━━━━━━━━
            Commit: `${{ github.sha }}`
            Branch: `${{ github.ref_name }}`
