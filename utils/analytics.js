/**
 * Lightweight analytics helper. Replace with real analytics SDK when available.
 * @param {string} eventName - Name of the analytics event.
 * @param {Record<string, any>} payload - Event payload metadata.
 */
export const logAnalyticsEvent = (eventName, payload = {}) => {

};
