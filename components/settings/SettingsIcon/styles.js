import { StyleSheet } from "react-native";

export default StyleSheet.create({
  iconWrapper: {
    width: 45,
    height: 45,
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
  },
});
