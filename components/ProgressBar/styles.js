import { StyleSheet } from "react-native";

export default StyleSheet.create({
  track: {
    width: "100%",
    borderRadius: 8,
    overflow: "hidden",
  },
  fill: {
    height: "100%",
    borderRadius: 8,
  },
});
