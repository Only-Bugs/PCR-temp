/**
 * @fileoverview Route screen for logging transport activity.
 * Wraps the LogTransportActivity component.
 */

import LogTransportActivity from '../components/tracking/LogActivity/LogTransportActivity';

export default LogTransportActivity;
